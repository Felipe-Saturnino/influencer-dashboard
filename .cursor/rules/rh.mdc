---
description: Regras para a secção RH (menu). Complementa global.mdc e brand-css-variables.mdc.
globs:
  - src/pages/rh/GestaoPrestador/**/*.tsx
  - src/pages/rh/GestaoPrestador/**/*.ts
  - src/pages/rh/DadosCadastro/**/*.tsx
  - src/pages/rh/DadosCadastro/**/*.ts
  - src/pages/rh/Organograma/**/*.tsx
  - src/pages/rh/Organograma/**/*.ts
  - src/pages/rh/Vagas/**/*.tsx
  - src/pages/rh/Vagas/**/*.ts
  - src/pages/rh/CentralDenunciasSpin/**/*.tsx
  - src/pages/rh/CentralDenunciasSpin/**/*.ts
alwaysApply: false
---

# Seção RH

**Antes de implementar:** `global.mdc` → `brand-css-variables.mdc` → este ficheiro.

**Marca / filtros / headers:** Brand MDC (`PageHeader` 28/22 nas páginas de gestão). **`title` + `subtitle`:** `h1` = `label` em `menu.ts`; subtítulo obrigatório em páginas novas — Brand §4. Ajuda: `geral.mdc` (nome do menu). Filtro de operadora em **Gestão de Escala** e **Gestão de Staff:** `FiltroOperadoraSelect` — agregadora **Todas Operadoras**; `extraOptions` com **Nenhuma** quando o domínio exigir (ver Global).

**Nota:** páginas de **Escala** (`GestaoEscala`, `GestaoStaff`, `Calendario`) estão em `escala.mdc` — não duplicar aqui.

---

## Identificação

| PageKey | Label menu | Caminho |
|---------|------------|---------|
| `rh_funcionarios` | Gestão de Prestadores | `rh/GestaoPrestador/` |
| `rh_dados_cadastro` | Dados de Cadastro | `rh/DadosCadastro/` |
| `rh_organograma` | Organograma | `rh/Organograma/` |
| `rh_vagas` | Vagas | `rh/Vagas/` |
| `rh_central_denuncias` | Central de Denúncias | `rh/CentralDenunciasSpin/` |

Canal público de denúncia (formulário externo): `geral.mdc` → `public/CanalDenunciasSpinPage.tsx`.

---

## Gestão de Prestadores (`rh_funcionarios`)

- Pasta `rh/GestaoPrestador/`: `index.tsx` (página + modais), `gestaoPrestadorHelpers.ts`, `usePrestadorLista.ts` (carga, organograma, filtros, ordenação, KPIs de resumo), `RhFuncModalHeaderDetalhes.tsx`, `PrestadorKpiResumo.tsx`, `PrestadorFiltroBar.tsx`, `PrestadorTabelaColaboradores.tsx`.
- `PageHeader` (`rh/GestaoPrestador/index.tsx`); subtítulo obrigatório.
- **Ordem do topo:** alertas globais → **cards consolidado** (`app-grid-3`: Total, Cadastro incompleto, Revisão cadastral pendente) → barra de filtros → conteúdo da aba.
- **Barra de filtros:** componente `PrestadorFiltroBar.tsx` — mesma estrutura (`getFilterBarWrapperStyle` + três fileiras): Diretoria/Gerência/Setor/Contrato/Status; busca; abas + CTAs (`Novo Prestador` / `RH Talks` conforme aba e permissão).
- Sem filtro de operadora nesta página.
- **Tabela (Head Count, Ações de RH, Anotações RH):** `PrestadorTabelaColaboradores.tsx` — uma única tabela com flags por aba (`preencherAcoesHeadcount`, `tabelaAcoesRh`, `tabelaAnotacoesRh`, `tabelaSemSalario`); padrão canónico `useDataTableBlock()`; Remuneração com Eye/EyeOff quando `podeVerDadosSensiveis`; ações em ícone na célula.
- CTA gradiente em ações primárias de linha quando aplicável.
- **Handoff:** abas, headcount, fluxos de aprovação RH.

---

## Dados de Cadastro (`rh_dados_cadastro`)

- `PageHeader`; subtítulo **Próprios:** «Atualização cadastral — apenas o seu cadastro» · **Ver Sim:** «Consulta e atualização cadastral de prestadores.»
- **Permissões (Gestão de Usuários → `rh_dados_cadastro`):**
  - **Ver/Editar Próprios:** entra direto no cadastro vinculado ao e-mail de login; sem filtro Staff nem botão **Meu Cadastro**.
  - **Ver Sim + Editar Próprios:** barra com `FiltroCalendarioStaffSelect` (`mode="single"`) + **Meu Cadastro** (`FiltroMeuCalendarioButton`); abas na linha 2 do bloco (`getPageFilterBoxStyle`); cadastro alheio **somente leitura**.
  - **Ver Sim + Editar Sim:** mesma barra; edita abas **Dados cadastrais**, **Documentos**, **Formação e Competências** e **Experiência Profissional** de qualquer prestador ativo/indisponível (sem depender de Gestão de Prestadores).
- **Prestador encerrado:** abas Formação e Competências e Experiência Profissional em somente leitura (RLS + UI).
- **Aba Formação e Competências:** quatro blocos — formação acadêmica, idiomas (catálogo fixo), cursos/certificações e portfólio (link ou arquivo no bucket `rh-prestador-self-media`, até 15 MB; vídeo/áudio só por URL). CRUD gera linha no histórico (`tipo: formacao_competencias`). **Sem** revisão cadastral de 6 meses nesta aba.
- **Aba Experiência Profissional:** empregos **anteriores** (não confundir com **Histórico de trabalho**, read-only — vínculo Spin). Tabela Cargo · Empresa · Período (`jan/2022 — atual` quando sem data fim). CRUD → histórico (`tipo: experiencia_profissional`). **Sem** revisão 6 meses.
- **Aba Histórico de trabalho:** sempre read-only para todos — contratação/organograma/remuneração só em **Gestão de Prestadores**.
- **Revisão cadastral (6 meses):** banner e «Confirmar sem alterações» só no **próprio** cadastro.
- **Histórico (aba):** anotações RH «Particulares» ocultas para o prestador no próprio cadastro; visíveis ao consultar terceiro com Ver Sim.
- **RLS:** migration `20260602140000_rh_dados_cadastro_escopo_sim.sql` — `_rh_dados_cadastro_perm_val`; trigger preserva campos de contratação em edições via esta página.
- Helpers: `lib/rhDadosCadastroHelpers.ts`, `lib/rhFormacaoCompetenciasConstants.ts`, `lib/rhFormacaoHistorico.ts`, `lib/rhExperienciaProfissionalConstants.ts`, `lib/rhExperienciaHistorico.ts`, `lib/rhExperienciaDates.ts`.
- Migrations: `20260602160000_rh_formacao_competencias.sql`, `20260602180000_rh_experiencia_profissional.sql`.

---

## Organograma (`rh_organograma`)

- Árvore de times/cargos; componentes de organograma em `lib/`.
- Barra de diretorias (`OrgFiltroBarDiretorias`): carrossel + pill **Todas as diretorias** via `FiltroBarPillButton` (mesmo contrato que Histórico na barra), ícone `FilterBarIcons.diretoria` (`Building2`); `aria-pressed` quando agregador ativo.
- Painel (`getPageContentBoxStyle`): **Visualização** — `SectionTitle` «Diretorias» + `sub` em `OrgChartHierarquico`; **Gerenciamento** — `SectionTitle` «Gerenciamento» (`compact`) + `CtaCriarButton` **Nova diretoria** na mesma linha (`perm.canEditarOk`), accordion sem título legado.
- **Handoff:** edição, filtros, export.

---

## Vagas (`rh_vagas`)

- `PageHeader` + `RhVagasFiltroBar` (`getFilterBarWrapperStyle`): **Linha 1** abas (sem título/subtítulo por aba no painel); **Linha 2** `BarraPesquisaPagina` + filtros contextuais.
- **Abertas / Em andamento:** só pesquisa na linha 2.
- **Gerenciamento:** pesquisa + `FiltroBarCampoSelect` status (`ShieldEllipsis`, agregadora **Todos Status**) + `CtaCriarButton` **Nova Vaga** à direita.
- **Candidaturas:** pesquisa + status + tipo (`ClipboardType`, **Todos Tipos**) + vagas (`ClipboardType`, **Todas Vagas**); estado dos filtros na página; opções de vaga vindas de `RhVagasCandidaturasPainel` via callback; kanban sem bloco de filtros duplicado.
- Constantes: `lib/rhVagasFiltroConstants.ts`.
- **Handoff:** estados, publicação, candidatos.

---

## Central de Denúncias (`rh_central_denuncias`)

- Gestão interna de protocolos (distinto do canal público).
- Barra de filtros (`getFilterBarWrapperStyle`): **Linha 1** — carrossel de mês (`carouselNavStyles`, maio/2026 → atual; rótulo **Todo o período** em Histórico) + `FiltroHistoricoButton` + tipo (`FiltroEntidadeBarSelect`, `TriangleAlert`, agregadora **Todos Tipos**, multi `overlaps`) + status (`FiltroBarCampoSelect`, `FilterBarIcons.status`, **Todos Status**); **Linha 2** — `BarraPesquisaPagina` no relato.
- **KPIs** (Relatado, Em avaliação, Procedente, Não procedente): formato Financeiro — `app-grid-kpi-4`, `getPageKpiSectionGapStyle`, rótulo caixa alta, `borderLeft` 3px semântico, `brand.blockBg`, valor em `FONT_TITLE` (sem caixa pai).
- Modais: `ModalsVerHist`, `ModalsAtender` — a11y Global.
- **Handoff:** SLA, status, atendimento, histórico.

---

## Anti-patterns (RH)

- Documentar Gestão de Escala/Staff/Calendario neste ficheiro — usar `escala.mdc`.
- Ações de linha só com texto (sem ícone + `aria-label` + `title`).
- Confundir Central interna com página pública do Canal.
