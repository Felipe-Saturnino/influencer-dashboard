---
description: Regras para páginas Geral (shell, auth, ajuda) e Canal de Denúncias público. Complementa global.mdc e brand-css-variables.mdc.
globs:
  - src/pages/geral/**/*.tsx
  - src/pages/geral/**/*.ts
  - src/pages/public/CanalDenunciasSpinPage.tsx
alwaysApply: false
---

# Seção Geral

**Antes de implementar:** `global.mdc` → `brand-css-variables.mdc` → este ficheiro.

**Marca / filtros / headers de páginas logadas:** Brand MDC — não duplicar aqui.

---

## Identificação

| PageKey / rota | Caminho | Notas |
|----------------|---------|--------|
| Home | `geral/Home/` | Hub pós-login |
| Login | `geral/Login/` | Auth; brandguide no contexto |
| Configurações | `geral/Configurações/` | Conta, tema, perfil |
| Ajuda | `geral/Ajuda/` | Textos por página + glossário |
| Trocar senha obrigatória | `geral/TrocarSenhaObrigatorio/` | Gate pós-login |
| Canal de denúncias (público) | `public/CanalDenunciasSpinPage.tsx` | Rota pública; sem shell logado |
| Painel de notícias (público) | `public/PainelNoticiasPage.tsx` | Rota `/painel-noticias`; RSS TV; sem shell logado |

Não há secção no menu lateral — páginas acessadas por fluxo, atalho ou URL pública.

---

## Ajuda e glossário

**Ficheiros:** `geral/Ajuda/index.tsx`, `geral/Ajuda/glossarioData.ts`, painéis por `pageKey`.

**Regra (também no Global):** qualquer alteração de produto em **qualquer secção** deve revisar se Ajuda/glossário precisa de entrada nova, atualização ou remoção.

### Nomenclatura — sempre o nome do menu

A **fonte de verdade** do nome visível da página é `src/constants/menu.ts` (`label` do item).

| Onde | Regra |
|------|--------|
| `CONTEUDO_CONHECA` / `CONTEUDO_TROUBLE` | Campo **`titulo`** = mesmo texto que `label` no menu (ex.: `Resultados`, `Feedback`, `Agenda`) |
| Troubleshooting transversal | `TROUBLESHOOTING_TRANSVERSAL` em `conteudo/troubleshooting.ts` — blocos de plataforma (ex.: busca sem acentos) no topo da aba Troubleshooting, antes do conteúdo da página selecionada |
| Navegação interna (Conheça / Troubleshooting) | Derivada de `MENU` em `menu.ts` — mesma ordem de secções e páginas; filtro `can_view` igual à Sidebar; glossário sem filtro por página |
| `glossarioData.ts` | Referências no corpo do termo (“na página **Resultados**”) — não “Resultado de Lives” |
| Texto que cita subtítulo da UI | Copiar o subtítulo **atual** da página (`PageHeader` / parágrafo sob `h1`), não versão antiga |

**Proibido** inventar títulos longos na Ajuda quando o menu já tem nome curto (`Feedback de Lives`, `Agenda de Lives`, etc.), salvo **exceção documentada** no MDC da secção.

Ao renomear item no menu, atualizar em cascata: `menu.ts`, `h1` da página, `titulo`/`label` na Ajuda, glossário e MDC da secção (tabela de subtítulos canónicos).

### Copy em PT-BR (Ajuda e glossário)

Seguir **Global § Linguagem e copy (PT-BR)**. Regras específicas:

| Contexto | Regra |
|----------|--------|
| Permissões na Ajuda | **permissão de Ver / Criar / Editar / Excluir** — nunca `can_editar`, `can_criar`, etc. |
| Loading citado na Ajuda | **`Carregando…`** (elipse Unicode) — alinhado à FilterBar e boot |
| Erros citados na Ajuda | Fecho **`entre em contato com o suporte`** |
| Dealers / cassino | **cassino ao vivo**; badge/campo **Live Cassino** |

### Ao adicionar página no menu

1. Entrada em `glossarioData.ts` (e bloco em `index.tsx` se houver tour textual).
2. Termos novos de domínio no glossário.
3. `titulo` e `label` na Ajuda = `label` de `menu.ts`.
4. Handoff pode completar corpo dos blocos — estrutura e nomenclatura ficam neste MDC + Brand §4.

### Handoff pendente

- Revisão contínua de cobertura: todas as `PageKey` do `menu.ts` com texto de ajuda adequado.

---

## Home (`geral/Home/`)

- Ponto de entrada após login.
- **`investidor`:** layout dedicado em `HomeInvestidor.tsx` — boas-vindas (sem avatar), KPIs mesas MTD (`relatorio_daily_summary` + lista por operadora), aquisição Streamers/Mídias, Spin na Rede (MTD, oculto se vazio), informativos publicados (`perfis` contém `investidor`, HTML integral), atalhos fixos com subtítulo de `lib/pageCanonicalCopy.ts`.
- **`executivo`:** layout dedicado em `HomeExecutivo.tsx` — mesma estrutura que Investidor (KPIs, aquisição, Spin na Rede, atalhos Agenda/Overview Spin/Streamers/Ajuda); boas-vindas com subtítulo executivo; informativos com `perfis` contém `executivo`. RLS: `_role_sem_escopo_app()` em `20260526120000_home_investidor_feed_rls.sql`.
- **`prestador`:** layout dedicado em `HomePrestador.tsx` — boas-vindas; blocos de **aniversário pessoal** (`data_nascimento`) e **aniversário de empresa** (`data_inicio`, dia/mês vs hoje) logo abaixo — ambos no mesmo dia: pessoal primeiro, empresa abaixo; ícones Lucide (Brand §4/§6); bloco **Informações** (informativos `prestador` + cards de novidade da Galeria de Fotos — gerais por evento e Minhas Fotos, janela 30 dias em `homePrestadorGaleriaNovidades.ts`); atalhos Ajuda + Organograma; Spin na Rede MTD. RLS: `20260527200000_home_prestador_feed_rls.sql` + galeria via políticas existentes; datas via `buscarRhFuncionarioAtivoPorEmailLogin` (mesmo critério Dados de Cadastro).
- **`operador`:** `HomeOperadorRouter` lê `operadoras.home_template` do escopo (`operadoraBrand`). **`default` / null / chave desconhecida → `HomeOperadorPadrao`** (boas-vindas, KPIs MTD com comparativo MoM, Spin na Rede, informativos com `operador_escopo`, atalhos Overview Spin + Ajuda). Templates dedicados: `lib/homeOperadoraTemplate.ts` + `operador/templates/`; campo na Gestão de Operadoras → aba Operações.
- **Dados Home Investidor/Operador:** não dependem de `usePermission` das páginas fonte; RLS em `supabase/migrations/20260526120000_home_investidor_feed_rls.sql` e `20260526140000_home_operador_feed_rls.sql` (aplicar no Supabase).
- **Período KPI / Aquisição (Home Investidor, Executivo e Operador Padrão):** `getHomeKpiReferenciaMes` / `getHomeKpiPeriodo` em `lib/homeInvestidorMtd.ts` — dia 1 do mês civil → mês anterior (ETL D-1); demais dias → MTD do mês de referência até ontem. Subtítulo do bloco: `HomeSectionMesSubtitle` com label `"Maio 2026"`.
- **Demais perfis:** Home legada (`index.tsx`) — atalhos dinâmicos pelo menu + blocos influencer/agência.

---

## Login (`geral/Login/`)

- Fora do `app-page-shell` logado.
- CTA com gradiente Brand; spinner `#fff` dentro do botão.
- **Esqueci senha:** link «Esqueceu sua senha?» abaixo do campo senha → modal «Reset de senha» → Edge `recuperar-senha` (senha padrão + e-mail Resend + `must_change_password`). E-mail inexistente: mensagem explícita + link «entre em contato» (`lib/loginAccessContact.ts`).
- **Handoff:** fluxos whitelabel adicionais, erros inline.

---

## Configurações (`geral/Configuracoes/`)

- Preferências de conta/tema; CTAs de marca quando aplicável.
- **Handoff:** abas e campos por perfil.

---

## Trocar senha obrigatória (`geral/TrocarSenhaObrigatorio/`)

- Fluxo bloqueante; mesmo padrão de CTA/spinner do Login.
- **Handoff:** validações e mensagens de erro.

---

## Painel de notícias — público (`public/PainelNoticiasPage.tsx`)

**Exceções ao padrão de página logada** (mesmo espírito do canal de denúncias):

- Rota **`/painel-noticias`** — `isPainelNoticiasPublicPath()` / `detectPublicUnauthenticatedRoute()`.
- **Sem** login, menu, `PageHeader`, `usePermission`.
- Dados: `painel_noticia` via anon (RLS); ingestão RSS horária (`sync-painel-noticias-rss`).
- UI: fundo dark fixo, assunto + detalhe (sem URL/imagem), carrossel vertical **20 s** por slide (sobe), polling 60 s.
- Purge físico na base após janela de 4 h quando fora do conjunto mínimo de 5 exibíveis.

---

## Canal de denúncias — público (`public/CanalDenunciasSpinPage.tsx`)

**Exceções ao padrão de página logada:**

- **Sem** `usePermission` / menu lateral / `PageHeader` Spin logado.
- **Sem** matriz Gestão de Usuários — acesso por URL pública (`isCanalDenunciasPublicPath`).
- Tema: pode usar tokens neutros e identidade Spin fixa; whitelabel operador **não** se aplica como nas páginas internas salvo decisão de produto.
- Formulários: `CampoObrigatorioMark` e semântica de erro do Global.
- Privacidade: não expor detalhes técnicos em mensagens de erro.

**Handoff:** campos do formulário, anonimato, confirmação pós-envio, a11y embed.

---

## Anti-patterns (Geral)

- Duplicar regras de dashboard/operacional neste MDC.
- Canal público com `usePermission` ou copy de página interna sem adaptar.
- Alterar página de produto sem rever `glossarioData.ts` / Ajuda.
- **`titulo` na Ajuda** ou referência no glossário com nome diferente do menu (`menu.ts`).
- Citar subtítulo desatualizado na Ajuda após mudança no `PageHeader` da página.
- GameIcons no conteúdo de ajuda quando Lucide bastar.
