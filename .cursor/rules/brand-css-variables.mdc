---
description: MDC Brand — whitelabel, tokens CSS, cabeçalhos, filtros com ícone, carrossel de período, ações em ícone e CTAs. Complementa global.mdc; secções citam este ficheiro para detalhes de marca (não duplicar).
alwaysApply: true
---

# MDC Brand — Whitelabel e padrões de UI de marca

**Escopo:** identidade visual da operadora, componentes de filtro com ícone, navegação de período, cabeçalhos de página, **toolbars com botões em texto**, ações em ícone em tabela/lista e CTAs primários. **Não** cobre regras de negócio por página (ver MDCs de secção), **semântica de dados** (ver `global.mdc`) nem **build Vite / chunks / deploy Cloudflare** (ver `global.mdc` § Build Vite — ex.: `lucide`/`react-icons` em `vendor-react`, sem chunk `vendor-icons`).

**Relação com outros MDCs**

| Origem | Pode citar |
|--------|------------|
| **Global** | Este MDC (marca, filtros, headers, ações) + semântica, permissões, ordenação de tabelas, comportamento de filtros (`Todos`, busca >5) |
| **Brand (este)** | Global (limite semântica vs marca); MDC **Dashboards** (KpiCard, funil, A/B, MoM) |
| **Secções** (`dashboards`, `lives`, `estudio`, …) | **Global** + **Brand** — sem citar outras secções para marca |

**Antes de codar:** ler Global → Brand → MDC da secção (ordem detalhada no Global).

---

## 1. Whitelabel — mecanismo

- `AppContext` chama `aplicarBrandguide()` / reset e injeta variáveis em `document.documentElement`.
- **Ativo:** perfil **operador** + `operadoraBrand` → `brand.useBrand === true` em `useDashboardBrand()`.
- **Inativo:** defaults Spin em `src/styles/global.css` (`:root`); fallbacks nos `var(--brand-*, #…)` garantem identidade sem `if` por página.
- **Fonte:** `--brand-fontFamily` + `@font-face` quando houver `font_url` da operadora.
- **Logo:** `operadoraBrand` na Sidebar — não reimplementar fetch aqui.

### Dois conjuntos de tokens (`:root` + injeção)

| Conjunto | Tokens principais | Onde usar |
|----------|-------------------|-----------|
| **Canónico** | `--brand-action`, `--brand-contrast`, `--brand-bg`, `--brand-text`, `--brand-icon-color`, `--brand-action-12`, `--brand-action-border`, `--brand-surface`, `--brand-surface-hi`, `--brand-contrast-12/30` | `src/pages/dashboards/**` e código novo analítico |
| **Legado** | `--brand-primary`, `--brand-secondary`, `--brand-accent`, `--brand-icon`, `--brand-background`, `--brand-success`, `--brand-extra1…4`, `--brand-danger` | Login, menus, páginas fora de `dashboards/` |

Aliases no contexto: ao aplicar brandguide, `--brand-primary` = `--brand-action`, `--brand-secondary` / `--brand-accent` = `--brand-contrast`.

**Tokens de gráfico** (`--brand-extra1…4`, `--brand-danger`, `--brand-success`): mapeados a semântica de chart no `AppContext` — **não** substituir KPIs de GGR/ROI pela paleta semântica fixa do Global; ver `dashboards.mdc` para uso em gráficos.

### Mapa pasta → tokens

| Pasta | Tokens preferidos | Hook |
|-------|-------------------|------|
| `src/pages/dashboards/**` | `--brand-action`, `--brand-contrast`, `--brand-icon-color` | `useDashboardBrand()` |
| Demais `src/pages/**` | Legado + hook (`primary` → action quando `useBrand`) | `useDashboardBrand()` em toda página com filtro/header/CTA de marca |

**Proibido em código novo fora de `dashboards/`:** usar `--brand-action` / `--brand-contrast` como fallback principal em páginas operacionais — usar `--brand-primary` / `--brand-accent` ou o hook.

**Dentro de `dashboards/`:** não usar `--brand-primary` em código novo; não usar `rgba(124,58,237,…)` fixo.

---

## 2. Hook `useDashboardBrand()`

Import: `import { useDashboardBrand } from "../hooks/useDashboardBrand"` (ajustar `../`).

Obrigatório em páginas com header de marca, barra de filtros transparente, CTA primário ou tabela com ações brand-aware.

| Propriedade | Uso |
|-------------|-----|
| `useBrand` | Ramo whitelabel vs Spin default |
| `primary` | Título `h1`, ênfase de texto de marca |
| `secondary` / `accent` | Destaque secundário, chip ativo, borda de filtro selecionado |
| `blockBg` | Fundo de **cards/modais** — **não** na barra de filtros |
| `primaryIconBg` / `primaryIconBorder` / `primaryIconColor` | Badge 28×28 ou 32×32 do header |
| `primaryTransparentBg` / `primaryTransparentBorder` | **Barra de filtros** (strip transparente) |

Helpers partilhados: `getCtaCriarGradient` / `CtaCriarButton` (`lib/ctaCriarStyles.ts`, `components/CtaCriarButton.tsx`).

---

## 3. CTAs, chips e `color-mix`

### CTA de criação na página (Nova Live, Adicionar, Criar, …)

**Componente obrigatório:** `CtaCriarButton` — texto visível + ícone **`Plus` 14px** (Lucide); **não** prefixar `+` no label.

| Propriedade | Valor |
|-------------|--------|
| `borderRadius` | **10** (mesmo raio das **abas** Overview Spin — não pill 999 dos filtros) |
| `padding` | **10px 20px** (referência Nova Live / Agenda) |
| `fontSize` / `fontWeight` | **13** / **700** |
| `gap` ícone–texto | **6** |
| Cor do texto | **`#fff`** |
| Gradiente **Spin** (todos os perfis sem whitelabel) | `linear-gradient(135deg, var(--brand-primary, #4a2082), var(--brand-secondary, #1e36f8))` — referência Scout |
| Gradiente **operadora** (`useBrand`) | `linear-gradient(135deg, var(--brand-action), var(--brand-contrast))` |

```tsx
import { CtaCriarButton } from "../components/CtaCriarButton";

<CtaCriarButton onClick={abrirModal}>Nova Live</CtaCriarButton>
```

- `loading` + `loadingLabel` quando houver verificação assíncrona (ex.: Agenda).
- `disabledBackground` só quando o domínio exigir cinza em `disabled` (ex.: Status Técnico).
- **Salvar** em modais e outros CTAs secundários: fora deste contrato até migração explícita.

### Gradiente CTA genérico (legado / modais)

```tsx
import { getCtaCriarGradient } from "../lib/ctaCriarStyles";

background: getCtaCriarGradient(brand);
```

- Fallback Spin nos `var()`: **#4a2082** → **#1e36f8** (alinhado ao Scout).
- Botão primário **sólido** (sem gradiente): só quando o layout exigir.

### Chip / pill ativo e inativo na barra

Estados partilhados em `lib/filterBarStyles.ts` (`getFiltroCampoInativoStyle`, `getFiltroCampoAtivoStyle`, `getFiltroBarPillStateStyle`) — usados por **Operadora**, **Influencer**, **Histórico** e **Hoje**.

| Contexto | Token no `color-mix` (outros chips/selects ativos) | Borda/texto ativo |
|----------|------------------------------------------------------|-------------------|
| `src/pages/dashboards/**` | `var(--brand-action, #7c3aed)` 15% | `brand.accent` ou `--brand-action` |
| Demais secções | `var(--brand-accent, #1e36f8)` ou `var(--brand-primary, #7c3aed)` 12–15% | `brand.accent` ou `brand.primary` |

Fallback Spin quando `!brand.useBrand`: `color-mix` com `var(--brand-primary, #7c3aed)` nos pills ativos (via `getFiltroCampoAtivoStyle`).

### Links de marca (não semânticos)

Preferir `var(--brand-primary, #7c3aed)` — evitar `brand.accent` do hook em links externos (contraste whitelabel).

### Spinners (`Loader2`)

1. **Dentro de CTA com gradiente** → `#fff`.
2. **Fora de botão**, fundo neutro → `var(--brand-primary, #7c3aed)` ou `var(--brand-action, #7c3aed)` se o ficheiro já estiver migrado.

Indicador secundário na FilterBar: `<Clock size={12} aria-hidden />` + texto “Carregando…” — ver Global.

---

## 4. Cabeçalho — título, subtítulo e famílias visuais

### Regra de conteúdo (obrigatória em toda página logada)

1. **Título (`h1`):** texto **idêntico** ao `label` do item no menu (`src/constants/menu.ts`) para a `key` da página — mesma grafia e capitalização (ex.: menu “Resultados” → título “Resultados”, não “Resultado de Lives”; menu “Agenda” → “Agenda”, não “Agenda de Lives”).
2. **Subtítulo:** **uma linha** fixa sob o `h1`, `color: t.textMuted`, `fontSize: 13`, `margin: "5px 0 0"`, `fontFamily: FONT.body`, descrevendo o propósito da página em linguagem de produto (não regras técnicas). **Obrigatório** em páginas novas e ao alterar cabeçalho em legado.
3. **Ícone do header:** **Lucide** (ou ícone já usado no menu da mesma página), `aria-hidden="true"`, `strokeWidth` default **2**. **GameIcons só no menu lateral** — não repetir Gi* no badge do header.

**Páginas novas — checklist de implementação:**

| Passo | O quê |
|-------|--------|
| 1 | Copiar `label` de `menu.ts` para `title` / `h1` |
| 2 | Definir subtítulo canónico (tabela da secção em `dashboards.mdc`, `lives.mdc`, etc., ou handoff aprovado) |
| 3 | Usar `PageHeader`, `DashboardPageHeader` ou header inline da secção **com** subtítulo no mesmo bloco visual do título |
| 4 | Atualizar Ajuda: `titulo` em `CONTEUDO_*` = mesmo nome do menu — ver `geral.mdc` |

**Exceções ao subtítulo único:**

| Caso | Padrão |
|------|--------|
| **Influencers / Afiliados** | Subtítulo condicional: gestão vs perfil próprio (textos canónicos no MDC da secção) |
| **Central de Notificações** | Subtítulo **fixo** no `PageHeader`; contador ou mensagem contextual num **segundo** `<p>` abaixo do header (formato dinâmico evolui depois) |
| **Playbook Influencers** | Subtítulo curto sob o `h1`; parágrafos introdutórios longos (`PLAYBOOK_SUBTITULO_PARAGRAFOS`) são corpo — não substituem o subtítulo |

**Anti-patterns de subtítulo:** janela técnica no lugar de valor (“após 5 h” só no texto de Ajuda se necessário); nome de tabela/RPC; duplicar o subtítulo no `subtitle` do `PageHeader` **e** num parágrafo igual logo abaixo; título com sufixo “de Lives” quando o menu não usa.

### Implementação — preferir componentes

| Família | Componente | Badge | `h1` | Onde |
|---------|------------|-------|------|------|
| **B — Operacional** | `PageHeader` ou `DashboardPageHeader` | 28×28, ícone 14px | 22px, `FONT_TITLE`, uppercase | Estúdio, Aquisição, Marketing, Plataforma, RH, Dashboards, Lives (maioria) |
| **A — Conteúdo compacto** | Header inline ou evoluir para `PageHeader` | 32×32, ícone 16px | 18px, `FONT_TITLE`, uppercase | Conteúdo (Playbook, Links), algumas vitrines |

**Exceções documentadas:** Portal RH (`conteudo/`) usa **Família B** (`PageHeader` 28/22) por alinhamento com gestão operacional.

Props mínimas:

```tsx
<PageHeader
  icon={<Icone size={14} aria-hidden="true" />}
  title="Gestão de Links"   // = menu.ts label
  subtitle="Mapeie UTMs detectados a influencers, afiliados ou campanhas e alimente os relatórios."
  actions={...}
/>
```

**Dashboards (parágrafo sob `h1`, mesmo estilo):**

```tsx
<p style={{ color: t.textMuted, fontFamily: FONT.body, fontSize: 13, margin: "5px 0 0" }}>
  Acompanhe performance, conversão e financeiro do canal de influencers.
</p>
```

**Conteúdo Família A (32/18):** subtítulo no `<p>` com `paddingLeft: 40` alinhado ao ícone, ou `PageHeader` se a página migrar para Família B.

**Nunca:** emoji no `h1`; ícone sem wrapper; `fontSize: 26` no título; página logada de produto **sem** subtítulo.

---

## 5. Barra de filtros

### Pill `borderRadius: 999` (padrão de controles na barra)

Filtros e toggles repetíveis na **barra de filtros** usam pill **999** por defeito — alinhado a **Histórico** (`FiltroHistoricoButton`), **Operadora** (`FiltroOperadoraSelect` com `pill`), **Influencer** (`FiltroInfluencerSelect`) e **novos filtros** da barra em código novo.

- **Não** usar `borderRadius: 10` ou `20` na barra salvo exceção documentada na secção.
- `SelectComIcone` **sem** `pill` permanece válido **fora** da barra (formulários, modais, comparativos A/B, domínios que não sejam filtro de lista na strip).

Wrapper obrigatório (dentro de `marginBottom` ~14–20):

```tsx
<div style={{
  borderRadius: 14,
  border: brand.primaryTransparentBorder,
  background: brand.primaryTransparentBg,
  padding: "12px 20px",
}}>
  <div style={{ display: "flex", alignItems: "center", justifyContent: "center", gap: 10, flexWrap: "wrap", width: "100%" }}>
    {/* carrossel, chips, selects, Histórico, loading secundário */}
  </div>
</div>
```

- **Alinhamento:** `justifyContent: "center"` + `width: "100%"` em cada linha de filtros do bloco (chips, carrossel, selects) — ver **Global** § Alinhamento no bloco de filtros. Alinhar à esquerda só com pedido explícito de produto.
- **Nunca** `brand.blockBg`, `t.cardBg` ou fundo sólido no strip.
- **Nunca** select de operadora/influencer **fora** deste wrapper.
- **Nunca** `justifyContent: "flex-start"` em fileiras de chips/status/plataforma na barra salvo exceção documentada no Global.

### CTA de criação **dentro** do bloco de filtros

Quando o fluxo **criar / adicionar / nova …** ficar na **mesma strip** de filtros (ex.: Scout, Network, Figurinos, Gerenciamento Portal RH):

| Regra | Valor |
|-------|--------|
| Componente | **`CtaCriarButton`** — ver §3 (não montar `<button>` inline na barra) |
| Forma / tamanho | **`borderRadius: 10`** (abas Overview Spin), **`padding: 10px 20px`**, `Plus` **14px** — **não** usar pill **999** nem padding **`6px 14px`** dos filtros |
| Posição | Na linha do bloco, `gap: 10` com carrossel, selects e toggles; centralização da fileira conforme Global § Alinhamento |
| Visual | Gradiente cheio (§3) — distinto do `color-mix` dos filtros **ativos** |

**Proibido na barra:** CTA de criação com `borderRadius: 999` (pill de filtro), label com `+` no texto em vez de ícone, ou gradiente inline duplicado.

**Fora do bloco** (card da Agenda, `PageHeader`, toolbar de tabela): mesmo **`CtaCriarButton`** — §3; só muda o wrapper pai, não o contrato do botão.

---

## 6. Iconografia em filtros e controles da barra

**Regra:** todo filtro ou controlo repetível na barra deve ter **ícone à esquerda** (Lucide, `aria-hidden`), com `aria-label` no controlo nativo. Preferir componentes partilhados em vez de markup duplicado.

### Componentes

| Componente | Caminho | Quando usar |
|------------|---------|-------------|
| **`FiltroOperadoraSelect`** | `src/components/FiltroOperadoraSelect.tsx` | **Único** padrão para filtro de **lista de operadoras** na barra (ver tabela e snippet abaixo) |
| **`FiltroHistoricoButton`** | `src/components/dashboard/FiltroHistoricoButton.tsx` | **Único** padrão para botão **Histórico** na barra (Overview Influencer) |
| **`FiltroInfluencerSelect`** | `src/components/FiltroInfluencerSelect.tsx` | **Único** padrão para filtro de **influencer(s)** na barra (`mode` single \| multiple) |
| **`FiltroModoVisualizacaoSelect`** | `src/components/FiltroModoVisualizacaoSelect.tsx` | Modo de vista de calendário na barra (Mês / Semana / Dia) — ícone `CalendarRange` 15px |
| **`FiltroHojeButton`** | `src/components/dashboard/FiltroHojeButton.tsx` | Atalho **Hoje** na Agenda — ícone `History` 15px (não confundir com Histórico) |
| `SelectComIcone` | `src/components/dashboard/SelectComIcone.tsx` | `<select>` único (**não** para operadora, influencer nem modo Mês/Semana/Dia na barra) |
| `SingleDropdown` | `src/components/dashboard/SingleDropdown.tsx` | **Legado** — não usar; preferir `FiltroModoVisualizacaoSelect` |
| `FiltroEntidadeMultiSelect` | `src/components/FiltroEntidadeMultiSelect.tsx` | **Legado** Staff/Time — não usar para influencers; refactor dedicado pendente |
| Input com ícone | Markup local | Busca, campos na barra — ícone `Search` absoluto à esquerda |

`SelectComIcone`: prop `label` = `aria-label` obrigatório; `pill` default **true** na barra; indicador ▾ embutido no componente. Espaçamento entre controlos na linha: **`gap: 10`** (`lib/filterBarStyles.ts`).

### Filtro de operadora — contrato visual (obrigatório)

Import: `FiltroOperadoraSelect` de `components/dashboard` (reexporta constantes `OPERADORA_FILTRO_TODAS_VALUE`, `OPERADORA_FILTRO_TODAS_LABEL`).

- Ícone **Shield** Lucide 15px dentro do campo (via `SelectComIcone` interno).
- Primeira opção do select: valor `todas`, texto visível **`Todas Operadoras`** (`OPERADORA_FILTRO_TODAS_LABEL`).
- `aria-label` do controlo: **`Operadoras`** (`OPERADORA_FILTRO_ARIA_LABEL`).
- **Sem** rótulo de texto ao lado (“Operadora”, “Filtrar por operadora”).
- Destaque quando slug específica selecionada: `highlightWhenFiltered` (default `true`) — `color-mix` com `--brand-action`.
- **`pill` default `true`** em `FiltroOperadoraSelect` / `SelectComIcone` na barra (referência: **Overview Influencer**); estado inativo: `t.text`, fundo `t.inputBg`, borda `t.cardBorder`.

```tsx
<FiltroOperadoraSelect
  value={filtroOperadora}
  onChange={setFiltroOperadora}
  operadoras={operadorasList}
  podeVerOperadora={podeVerOperadora}
/>
```

Regras de dados e `extraOptions` (“Nenhuma”): **Global** § Filtro de operadora.

### Catálogo de ícones por tipo de filtro

| Tipo de filtro | Ícone (Lucide) | Tamanho ref. | Componente / notas |
|----------------|----------------|--------------|-------------------|
| Operadora | `Shield` | 15 | **`FiltroOperadoraSelect`** (envolve `SelectComIcone`); opção agregadora **Todas Operadoras**; legado `GiShield` / markup duplicado proibido em código novo |
| Influencer (único ou múltiplo) | `User` | **15** no trigger | **`FiltroInfluencerSelect`** — agregadora **Todos Influencers**; pill 999 |
| Campanha / UTM | `Megaphone` ou `Link2` | 13–15 | `SelectComIcone` conforme domínio |
| Perfil / role (plataforma) | `UserCog` ou `Shield` | 13–14 | Preferir Lucide; legado `GiPerson` / `GiShield` em Gestão de Usuários |
| Status de workflow | `ListFilter` ou `CircleDot` | 13–15 | Chip ou `SelectComIcone` |
| Busca textual | `Search` | 14–16 | À esquerda do `<input type="search">`, `aria-label` no input |
| Modo Histórico (toggle) | `Calendar` | 15 | **`FiltroHistoricoButton`** — pill 999 + texto “Histórico”; ver Global § Botão Histórico |
| Modo de vista (Mês/Semana/Dia) | `CalendarRange` | 15 | **`FiltroModoVisualizacaoSelect`** — `getFiltroBarPillStateStyle`; inativo no `defaultValue` |
| Atalho Hoje (Agenda) | `History` | 15 | **`FiltroHojeButton`** — `getFiltroBarPillStateStyle`; ver Global § Botão Hoje |
| Carregando (secundário) | `Clock` | 12 | Texto “Carregando…” — não é filtro |

**Botão Histórico na barra:** usar **`FiltroHistoricoButton`** (Overview Influencer) — `Calendar` 15px + texto “Histórico”, `aria-pressed`, labels em `HISTORICO_FILTRO_ARIA_LABEL_*`. Não duplicar estilos inline nem trocar por `History` só por estética.

Estado ativo do select (valor ≠ agregador): em **operadoras**, preferir `FiltroOperadoraSelect` (já aplica destaque com `--brand-action`). Outros filtros (`SelectComIcone` manual): borda/fundo marca conforme chip ativo da página.

---

## 7. Carrossel de período

Incluir quando a página filtra por período navegável (mês, semana, dia, turno, diretoria, time, etc.). Integrar **dentro** da barra de filtros (secção 5), salvo layout de escala que use `MesCarrosselPeriodo` isolado.

**Implementação única:** `src/lib/carouselNavStyles.ts` — não duplicar estilos inline de setas/rótulo.

```tsx
import { getCarouselBtnNavStyle, getCarouselPeriodLabelStyle } from "../lib/carouselNavStyles";

<button
  type="button"
  aria-label="Mês anterior" // adaptar: Semana anterior, Turno anterior, Diretoria anterior, Período anterior…
  disabled={desabilitado}
  style={getCarouselBtnNavStyle(t, desabilitado)}
>
  <ChevronLeft size={14} aria-hidden="true" />
</button>

<span style={getCarouselPeriodLabelStyle(t, { minWidth: 180 /* ou clamp se nome longo */ })}>
  {labelPeriodo} {/* conteúdo do domínio — nunca hardcodar formato no helper */}
</span>

<button type="button" aria-label="Próximo mês" disabled={desabilitado} style={getCarouselBtnNavStyle(t, desabilitado)}>
  <ChevronRight size={14} aria-hidden="true" />
</button>
```

| Dimensão | Valor |
|----------|--------|
| Botões seta | 32×32, `Chevron` 14px, opacidade **0.35** quando `disabled` |
| Rótulo central | 18px / peso 800 / `FONT.body` / `minWidth` 180 (ajustar só se ellipsis necessário) |

| Contexto | `aria-label` setas | Rótulo central (exemplos) |
|----------|-------------------|---------------------------|
| Mês | `Mês anterior` / `Próximo mês` | Label do mês; modo Histórico → **`Todo o período`** |
| Semana | `Semana anterior` / `Próxima semana` | Label da semana |
| Período genérico (Agenda) | `Período anterior` / `Próximo período` | Título conforme vista (mês/semana/dia) |
| Turno | `Turno anterior` / `Próximo turno` | Label do turno |
| Diretoria / time | `Diretoria anterior` / `Próxima diretoria` (ou time) | Nome da diretoria/time ou agregador |

**Modo Histórico:** setas `disabled` + `getCarouselBtnNavStyle(t, true)`; botão Histórico com `aria-pressed={true}`.

**Referências:** `PortalRhBlocoFiltros`, Streamers FilterBar, Financeiro, Agenda, Feedback, `MesCarrosselPeriodo` (escala — card com `blockBg`, exceção de wrapper).

---

## 8. Ações — toolbar vs linha

Dois contextos distintos; **não** misturar o padrão de um no outro.

### Toolbar de página ou de bloco (ex.: barra acima da grade, canto do `SectionTitle`)

Ações de **fluxo da página** (**Nova Escala**, **Salvar Alterações**, **Sugestão de Escala**, **Aprovar**, **Gerar**, **Exportar**, **+ Criar**): botão **com texto visível** — **não** converter para somente ícone.

- Labels curtas e verbosas no botão (ex.: “Salvar Alterações”, não só “Salvar” se o contexto exigir clareza).
- **`aria-label`** pode repetir ou detalhar o texto visível quando o rótulo for ambíguo.
- **`title`** opcional; não substitui o texto do botão.
- CTAs primários de criação: preferir **gradiente** canónico (secção 3) + texto branco; secundários: borda `t.cardBorder` ou tint `color-mix` com `brand.accent` / `brand.primary`.
- Spinner em toolbar: dentro de CTA com gradiente → `#fff`; em botão tintado/neutro → cor do texto do botão ou `var(--brand-primary, #7c3aed)` no `Loader2` fora de gradiente.

**Referência:** toolbar da Escala Diária em `GestaoEscala` (Nova Escala, Salvar, Aprovar, Sugestão).

### Linha de tabela, card ou lista densa

Ações por registro (**Editar, Ver, Histórico, Aprovar, Arquivar, Excluir, …**): botão **somente ícone**, sem texto visível por defeito.

- **`aria-label` descritivo** obrigatório (ex.: `` `Editar ${nome}` ``).
- **`title`** com o mesmo texto (ou rótulo curto) para tooltip no hover.
- Tamanho do ícone na célula: **13px** (tabelas densas) ou **14px**.
- Estilo base: quadrado ~28–32px, `borderRadius: 8`, borda `t.cardBorder`, fundo `t.inputBg` — referência `btnAcao` em `GerenciamentoPostagens`, `btnIconTabela` em RH.

### Catálogo de ações comuns (Lucide) — uso em **linha**

| Ação | Ícone | `aria-label` (padrão) |
|------|-------|------------------------|
| Editar | `Pencil` | `Editar {identificador}` |
| Ver / Visualizar | `Eye` | `Ver {identificador}` |
| Histórico / thread | `Clock` ou `History` | `Histórico de {identificador}` |
| Aprovar | `Check` | `Aprovar {identificador}` |
| Arquivar | `Archive` | `Arquivar {identificador}` — 2º clique: incluir “Confirmar arquivamento” no label |
| Excluir | `Trash2` | `Excluir {identificador}` — 2º clique: “Confirmar exclusão” |
| Link externo | `ExternalLink` | `Abrir {destino} em nova aba` |
| Expandir linha | `ChevronRight` rotacionado | Nunca `▶` textual |

**Exceções em linha (só com pedido explícito de produto):** confirmação em 2 cliques pode mostrar microtexto “Confirmar?” ao lado do ícone (Portal RH Arquivar).

**Legado a migrar (linha):** botões com texto “Ver”, “Editar”, “Aprovar” em células de tabela (ex. Afiliados) → ícone + `aria-label` + `title`. **Não** migrar toolbars de fluxo para ícone.

**Legado a migrar (toolbar):** ícone sem texto em ações de fluxo (Nova Escala, Aprovar bloco, etc.) → restaurar **texto visível**.

**Ações semânticas de negócio** (Pago, Recusado, Liberar banca): podem manter cor semântica (`#22c55e`, `#e84025`) — não confundir com token de marca; ver Global.

---

## 9. O que não é marca (permanece no Global)

- Paleta semântica de dados (verde = bom, vermelho = ruim, amarelo, roxo FTD/Hora).
- `useApp()` / `t.*` para superfícies neutras.
- `CampoObrigatorioMark`, classes `app-*`, `tableStyles`, `SortTableTh`, ordenação default, permissões (Gestão de Usuários / admin irrestrito), filtros (`Todos` default, busca se >5 opções), strings de vazio/loading.
- Título alinhado ao menu + subtítulo: regra de conteúdo no Global; implementação visual neste MDC (secção 4).

---

## 10. Anti-patterns (marca)

- `rgba(124,58,237,…)` fixo em `dashboards/` ou quando `brand.useBrand`.
- `brand.accent` em links externos.
- `GiShare` / GameIcons no header de página.
- Gradiente fallback com `#7c3aed` no início (usar `CtaCriarButton` / `getCtaCriarGradient`).
- CTA de criação inline sem `CtaCriarButton`; label com `+` em vez de ícone `Plus`.
- Filtro sem ícone quando existe entrada no catálogo (secção 6).
- Filtro de operadoras sem `FiltroOperadoraSelect` ou com agregadora “Todas as operadoras”, “Operadoras” (sem “Todas”) / label lateral “Operadora”.
- Filtro de influencers na barra sem `FiltroInfluencerSelect`, agregadora diferente de **Todos Influencers**, ou `SelectComIcone`/`InfluencerDropdown` legado.
- Controlo novo na barra de filtros com pill retangular (`borderRadius` 10/20) sem exceção documentada na secção.
- Botão Histórico na barra sem `FiltroHistoricoButton` ou com estilo inativo legado (`transparent`, `textMuted`).
- Ação de **linha** só com texto, sem ícone (código novo).
- Ação de **toolbar** só com ícone, sem texto (fluxo da página).
- Título diferente do menu ou página sem subtítulo.
- `blockBg` na barra de filtros.
- Substituir verde/vermelho de GGR/ROI por `--brand-primary` por estética.

---

## 11. Checklist rápido (página nova)

- [ ] `useDashboardBrand()` + header com título = `menu.ts` + subtítulo
- [ ] Barra de filtros com `primaryTransparent*` + ícones no catálogo
- [ ] Filtro de operadora: `FiltroOperadoraSelect` + opção agregadora **Todas Operadoras** (sem label lateral)
- [ ] Filtro de influencer(s): `FiltroInfluencerSelect` + agregadora **Todos Influencers** + pill 999
- [ ] Modo Mês/Semana/Dia (se aplicável): `FiltroModoVisualizacaoSelect` + `CalendarRange` 15px
- [ ] Carrossel de período (se aplicável) com `aria-label` nas setas
- [ ] Histórico: `FiltroHistoricoButton` + `aria-pressed` + rótulo carrossel `Todo o período`
- [ ] Toolbar com texto; ações de **linha** em ícone + `aria-label` + `title`
- [ ] CTA de criação na barra ou toolbar: **`CtaCriarButton`** (`borderRadius` 10, `10px 20px`, `Plus` 14) — não confundir com pills filtro `999` / `6px 14px`
- [ ] CTA primário com gradiente canónico; spinner `#fff` dentro do CTA
- [ ] Semântica de dado intacta (Global)
